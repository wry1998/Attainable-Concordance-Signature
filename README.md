# Attainable Concordance Signature on Swiss Market Index

Under the supervision of Prof. Johanna Nešlehová, I conducted an undergraduate research project motivated by McNeil et al. (2020) on attainable concordance signatures: the collection of concordance probabilities over the power set that characterizes multivariate dependence beyond pairwise measures. I proposed an applied study using the Swiss Market Index (SMI), which contains 20 constituents with two lack complete histories, making the attainable-set framework directly relevant.

---------------------------------------------------------

## Project Goal

The project aimed to answer the following questions:

- How can we study multivariate dependence when some components have partially or fully missing histories?
- How can we reduce dimensionality when the computation of concordance signatures scales exponentially with dimension? Is the commonly used **higher-order independence** simplification actually attainable in this dataset?
- What does the attainable set of concordance signatures look like on subsets of Swiss Market Index?

***************************************

## Data

The dataset consists of the **adjusted daily prices of the 20 SMI component stocks in 2020**, downloaded from Yahoo Finance. After cleaning:

- **18 stocks** had nearly complete histories. 
- **PGHN** had only a partial history, as it was newly added to the index after September 18, 2020
- **ALC** was treated as fully missing, since its price series was quoted in a different currency unit from the other stocks

### Preprocessing financial returns

To remove volatility-driven serial effects before dependence analysis, I first transformed prices into **log returns** and then fitted a **GARCH(1,1)** model to each marginal series using either **Gaussian** or **scaled-t innovations**.

Model adequacy was checked using:

- **auto-correlograms** of absolute residuals
- **Portmanteau tests** of strict white noise
- **QQ plots** and **histogram** for residuals to check Gaussian or scaled-t innovations

The resulting standardized residuals were treated as approximately **i.i.d.** and suitable for dependence analysis under an **elliptical distribution** framework.

------------------------------------------------------

## Methodology

### Attainable concordance signatures

For a set of variables \(D\), the **concordance signature** is the collection of concordance probabilities over subsets of \(D\). By McNeil et al. (2020), odd-order coordinates can be derived from even-order ones, so the signature is determined by its **even-subset concordance probabilities**.

The set of attainable signatures forms a **convex hull** generated by extremal mixture copulas.

Using this framework, I studied attainable sets for subsets involving:

- representative stocks from the 13 PEA clusters
- the incomplete component **PGHN**
- and, in a smaller experiment, both **PGHN** and **ALC**


### Dimension reduction via `Partial Exchangeability Assumption (PEA)`

A major computational obstacle is that the number of concordance-signature coordinates grows as **O(2^d)**.

To make the problem tractable, I applied the clustering algorithm of Perreault et al. (2019) to search for a partition satisfying the **Partial Exchangeability Assumption (PEA)**. Algorithm looks like follow:


This reduced the 18-stock panel to **13 clusters**, using the smallest number of clusters for which PEA was not rejected.

A useful practical observation from this step was that **industry groupings often contain several PEA-based clusters**, but they **rarely split a PEA-based cluster**. So, industry labels provide a strong initialization for the clustering search, while the data-driven PEA partition gives a more refined structure for dependence estimation.

--------------------------------------------------------

## Empirical Results



----------------------------------------------

## Limitations

The main limitation is computational.

Because the number of concordance-signature coordinates grows exponentially with dimension, the full 15-dimensional problem (13 clusters + PGHN + ALC) was too expensive to compute directly. As a result, the empirical study focused on carefully chosen lower-dimensional subsets.

A more complete analysis in larger dimensions would require either:

- stronger structural assumptions, or
- more efficient computational methods for vertex enumeration and convex-hull analysis


------------------------------------------------------------------

## Repository Contents

```text
.
├── data/               # cleaned or intermediate data files
├── code/               # R scripts for preprocessing, clustering, and attainability analysis
├── report/             # final written report
└── README.md
```

--------------------------------------------------

## References

